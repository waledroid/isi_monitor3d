__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
build/
dist/
.venv/
venv/

models/*.pt
models/*.onnx
models/*.engine
models/*.trt
*.engine
*.trt

calibration.json
config/local.yaml

*.mp4
*.mkv
*.avi
fixtures/recordings/

.vscode/
.idea/
*.swp

# training artifacts (multi-GB, never pushed)
trainer/isidet/runs/
trainer/isidet/data/
trainer/isidet/logs/
trainer/isidet/models/
trainer/isiGen/runs/
trainer/isiGen/data/
trainer/isidet/tests/.cache/
*.pt

# local scratch (recordings, old videos, slides)
archive/

# Multical isolated venv
calibration/.venv-multical/

# WSL Windows-download artifacts
*:Zone.Identifier

# Claude Code local settings
.claude/settings.local.json
.claude/scheduled_tasks.lock

# isical calibration Studio — captures, workspaces, job logs (not source)
isical/data/
isical/runs/

# runtime backups of the dashboard config store
config/*.yaml.bak

# local Claude Code state (never shared)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Claude worktrees are transient local checkouts
.claude/worktrees/
models/.trt_cache/
config/local_backups/

# thesis: heavy measurement byproducts stay out of git (the .md/.csv/log artifacts are tracked)
thesis/measurements/G6_ablation/arm_*/
thesis/measurements/G6_ablation/test_real/
thesis/measurements/G6_ablation/runs/*/weights/
# latex build renders (page-preview PNGs); the PDF and sources are tracked
thesis/latex/p*.png
thesis/latex/bg-*.png
thesis/latex/intro-*.png
thesis/latex/prog-*.png
