# isiGen Studio core — enough to run scripts/run_studio.py (:8200) and the
# non-generation pipeline stages. The heavy SDXL/LoRA generation stack lives
# in requirements-gen.txt (install only on a GPU machine that generates).
fastapi>=0.115
uvicorn>=0.30
jinja2>=3.1
pydantic>=2.6
pydantic-settings>=2.0
opencv-python>=4.8
numpy>=1.26
pyyaml>=6.0
