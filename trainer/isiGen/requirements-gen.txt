# The GENERATION stack (SDXL + depth ControlNet + LoRA) — lazy-imported by the
# generation/LoRA stages only. Install on the GPU machine:
#   pip install -r requirements.txt -r requirements-gen.txt
torch>=2.3
diffusers>=0.27
transformers>=4.40
accelerate>=0.29
peft>=0.10
safetensors>=0.4
Pillow>=10.0
onnxruntime>=1.17
